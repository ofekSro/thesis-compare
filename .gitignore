# Input data — backed up separately (NPZs/VTKs are ~8 GB), never committed.
# Copy these in manually per the README before running the pipeline.
data/

# Generated results — reproducible by re-running the pipeline
outputs/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS / editor
Thumbs.db
desktop.ini
.vscode/
.idea/
